# @package _global_

# Eight rollout replicas (one per GPU on an 8-GPU node), each processing batch
# size 16. The trajectory budget remains 128 * 4 = 512 per PPO step.
env:
  train:
    total_num_envs: 128
    rollout_epoch: 4

rollout:
  pipeline_stage_num: 1
